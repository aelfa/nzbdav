# Copy to .env for local development.
# `npm run dev` runs predev to sync this file from $CONFIG_PATH/.frontend-backend-api-key.

NODE_ENV=development
PORT=5173
BACKEND_URL=http://localhost:5000
FRONTEND_BACKEND_API_KEY=
